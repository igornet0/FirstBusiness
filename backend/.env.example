PORT=3001
FRONTEND_ORIGIN=http://localhost:5173
# Локальный PostgreSQL (для docker-compose см. DATABASE_URL в docker-compose.yml)
# DATABASE_URL=postgresql://firstbusiness:firstbusiness@localhost:5432/firstbusiness
JWT_SECRET=change-this-to-a-long-random-string-in-production

# DeepSeek (OpenAI-compatible API) — required for POST /api/ai
DEEPSEEK_API_KEY=sk-your-deepseek-key
